<div class="container">

 <%= link_to "Approve Application", approve_application_path(@application), method: :patch,
                          data: { confirm: "Are you sure you want to approve #{@application.user.first_name}'s application for #{@application.pet.name}?"}, class: "btn-yellow" , remote: true %>

</div>
