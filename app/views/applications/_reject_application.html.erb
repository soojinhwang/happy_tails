<div class="container">

 <%= link_to "Reject Application", reject_application_path(@application), method: :patch,
                          data: { confirm: "Are you sure you want to reject #{@application.user.first_name}'s application for #{@application.pet.name}?"}, class: 'btn-red', remote: true %>

</div>
