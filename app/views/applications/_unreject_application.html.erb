<div class="container">

 <%= link_to "Reverse Rejection", unreject_application_path(@application), method: :patch,
                          data: { confirm: "Are you sure you want to reverse the recision for #{@application.user.first_name}'s application on #{@application.pet.name}?"}, class: 'btn-red', remote: true %>

</div>
